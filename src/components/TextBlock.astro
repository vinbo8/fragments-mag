---
// import { published } from "@data/post"
import Wrapper from "@components/Wrapper";
const {frontmatter} = Astro.props;
---

<Wrapper title="Simple Wrapper">
    <div class="flex flex-col font-sans space-y-2">
		<span class="font-sans text-5xl text-center">{frontmatter.title}</span>
		<br class="my-2"/>
		<span class="font-serif text-lg"><slot /></span>
	</div>
	<hr class="mt-4">
</Wrapper>